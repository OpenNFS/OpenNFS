#version 330 core

// Interpolated values from the vertex shaders
in vec2 UV;
in vec3 inColor;

// Ouput data
out vec4 color;

void main(){
    color.r = inColor.r;
    color.g = inColor.g;
    color.b = inColor.b;
    color.a = 1.0;
}